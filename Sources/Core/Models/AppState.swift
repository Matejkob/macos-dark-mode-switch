import Foundation

// MARK: - App State
enum AppearanceMode: String, CaseIterable {
    case light = "light"
    case dark = "dark"
    case auto = "auto"
}

// MARK: - App Settings Model
struct AppSettings {
    // TODO: Define app-wide settings
}